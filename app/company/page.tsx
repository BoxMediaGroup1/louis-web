import { SimplePage } from "@/components/simple-page";

export const metadata = { title: "Company — Louis" };

export default function CompanyPage() {
  return (
    <SimplePage title="About Louis">
      <p>
        Louis is the secure workspace where legal teams run matters, review
        documents, and produce work they can stand behind.
      </p>
    </SimplePage>
  );
}
