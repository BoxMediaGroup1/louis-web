import { SimplePage } from "@/components/simple-page";

export const metadata = { title: "Security — Louis" };

export default function SecurityPage() {
  return (
    <SimplePage title="Security">
      <p>
        Louis is designed for confidential legal work: governed workspaces,
        permissioned knowledge, and an audit trail your firm can stand behind.
      </p>
    </SimplePage>
  );
}
