import { SimplePage } from "@/components/simple-page";

export const metadata = { title: "Customers — Louis" };

export default function CustomersPage() {
  return (
    <SimplePage title="Customers">
      <p>
        Leading firms use Louis to run matters, review documents and produce
        work they can stand behind.
      </p>
    </SimplePage>
  );
}
